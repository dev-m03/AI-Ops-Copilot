[pytest]
testpaths = tests
pythonpath = .
addopts =
    --tb=short
    -q
    --cov=.
    --cov-report=term-missing
    --cov-report=xml:coverage.xml

[coverage:run]
omit =
    tests/*

[coverage:report]
show_missing = true
