# Development / test-only dependencies for aiops-genai — do NOT add to requirements.txt
# Install with: pip install -r requirements-dev.txt  (run from aiops-genai/)
-r requirements.txt

pytest>=8.1,<9
pytest-mock>=3.14,<4
httpx>=0.27,<1          # required by FastAPI TestClient
